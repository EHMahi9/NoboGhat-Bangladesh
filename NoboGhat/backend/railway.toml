[build]
builder = "RAILPACK"
buildCommand = "chmod +x mvnw && ./mvnw -DskipTests package"

[deploy]
startCommand = "java -jar target/mahi-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
